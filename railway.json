{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate --schema=backend/prisma/schema.prisma && (npx tsc -p backend/tsconfig.json || exit 0)"
  },
  "deploy": {
    "startCommand": "npm --workspace=backend run start"
  }
}
